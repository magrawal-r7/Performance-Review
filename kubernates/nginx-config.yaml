apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-custom-config
data:
  custom.conf: |
    server {
      listen 80;
      location / {
        return 200 "Hello from ConfigMap NGINX!\n";
      }
    }
